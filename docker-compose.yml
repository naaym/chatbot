version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    volumes:
      - chroma_data:/app/chroma_db

volumes:
  chroma_data:
